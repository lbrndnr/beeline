[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
as-bytes = "0.2.0"
beeline = { path = "../beeline" }
bytes = "1.10.1"
libbpf-rs = "0.25.0"
libc = "0.2.173"
log = "0.4.28"

[build-dependencies]
libbpf-cargo = "0.25.0"
